---
description: Open one self-contained OverClick card.
argument-hint: <card-id>
---

Load the bundled `overclick` skill and read its linked canonical `OVERCLICK.md`.
Treat `$ARGUMENTS` as the exact card ID and call
`task_get`. Present the contract, harness, mission context, current status, and
branch convention without inventing missing requirements.
